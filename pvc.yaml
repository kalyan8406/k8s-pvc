apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: k8s-pvc
spec:
  storageClassName: k8sstrg
  accessModes: 
    -ReadWriteOnce
  resources:
    requests:
      storage: 1Gi  
  hostpath:
    path: /tmp/demo-pv 
